node_modules/
config.json
positions.json
*.log
.DS_Store
seen.json
# app-specific (holds YOUR fly app name + volume mount) - each install generates its own
fly.toml

# QTABLE live-test key file — NEVER commit
qtable-live.env
*.env

# fleet kill-switch OFFLINE private key — never commit
fleet-priv.pem
*.pem
